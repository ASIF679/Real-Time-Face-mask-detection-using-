def sumNumber(numbers):
    total = 0
    for num in numbers:
        total += num
    return total

# Example usage
numbers_list = [1, 2, 3, 4, 5]  # Example list of numbers
print(sumNumber(numbers_list))
